# Sample NODE JS App with Express
--- test commit
* Install Node and NPM on your machine
* Create the app.js file and import the express module
* Run the application 
   
    ```
    node app.js
    ```
* Check on the url 
   
   ```
   http://localhost:3000/
   ```
